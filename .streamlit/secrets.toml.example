# Streamlit Secrets Configuration Template
# Copy this to your Streamlit Cloud dashboard under "Secrets"
# DO NOT commit actual secrets to git!

# OpenAI API Configuration
OPENAI_API_KEY = "sk-proj-..."  # Your OpenAI API key
OPENAI_MODEL_NAME = "gpt-4o-mini"  # or "gpt-4o" for production

# Supabase Configuration (for cloud deployment)
SUPABASE_URL = "https://your-project-id.supabase.co"
SUPABASE_KEY = "your-anon-key-here"

# Notes:
# 1. Get your Supabase credentials from: https://app.supabase.com/project/_/settings/api
# 2. Use the "anon/public" key, NOT the service_role key
# 3. Make sure you've run the SQL schema from the deployment plan
# 4. For local development, you can continue using .env file (SQLite)
